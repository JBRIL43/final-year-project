services:
  - type: web
    name: student-debt-admin
    env: static
    rootDir: student-debt-admin
    buildCommand: npm install && npm run build
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
