# Mindmemo — Agent Bootstrap

You are working on **Mindmemo**. Do not plan or code until you have loaded context.

## Read first (in order)

1. **[`CONTEXT.md`](CONTEXT.md)** — full workspace map, workflow, rules, file index (**start here**)
2. **[`STATUS.md`](STATUS.md)** — what to do right now
3. **[`PROJECT_LOG.md`](PROJECT_LOG.md)** — history from project start (newest at top)
4. **[`CLAUDE.md`](CLAUDE.md)** — routing by task type
5. **[`standards/CONTEXT.md`](standards/CONTEXT.md)** + **[`planning/docs/project-origin.md`](planning/docs/project-origin.md)**

Then read the active workspace `CONTEXT.md` (`planning/`, `spec-definition/`, `development/`, or `publishing/`).

**Flutter implementation:** also [`../../flutter-standards/CONTEXT.md`](../../flutter-standards/CONTEXT.md), [`development/packages/ALLOWED_PACKAGES.md`](development/packages/ALLOWED_PACKAGES.md), and the active feature spec under `spec-definition/features/`.

## End of session (required)

Update [`STATUS.md`](STATUS.md) and append [`PROJECT_LOG.md`](PROJECT_LOG.md). See **End of every session** in [`CONTEXT.md`](CONTEXT.md).

## One-liner

Privacy-first iOS-first Flutter app: voice memos → on-device STT → local LLM (lazy) → per-memo chat → content conversion. No cloud inference. v1 = App Store approved.
